__pycache__/
*.pyc
*.egg-info/
dist/
build/
.eggs/
edgar-cache/
*.sqlite3
.DS_Store

# graphify-out/ -- code-graph build cache (~94 MB). Also indexes sources, so it
# can carry gated content past a source-level gate. Never commit.
graphify-out/

# paper/ -- UNPUBLISHED paper draft. Publishing it is a research decision, not
# repo cleanup, and OUTLINE.md carries Track 2 references. Deliberately ignored.
paper/
